import React from 'react'
import { Button } from '../ui/button'

const RewriteBtn = () => {
  return (
    <main>
        <Button className='w-full h-12'>
            Rewrite Note
        </Button>
    </main>
  )
}

export default RewriteBtn